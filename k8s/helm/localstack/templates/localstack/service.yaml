kind: Service
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-localstack
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Name }}-localstack
  ports:
    - protocol: TCP
      port: 4566
      targetPort: 4566
      name: http