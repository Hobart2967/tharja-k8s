apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ .Release.Name }}-enforce-https
  namespace: {{ .Release.Namespace }}
spec:
  redirectRegex:
    regex: "^http://(.*)"
    replacement: "https://$1"