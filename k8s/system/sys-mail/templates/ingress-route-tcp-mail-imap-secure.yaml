---
kind: IngressRouteTCP
apiVersion: traefik.io/v1alpha1
metadata:
  name: {{ .Release.Name }}-mail-imap-secure
  namespace: {{ .Release.Namespace }}
spec:
  entryPoints:
    - ml-imap-secure
  routes:
    - match: HostSNI(`*`)
      services:
        - name: mail-docker-mailserver
          port: imap-secure