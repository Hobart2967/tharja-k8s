---
{{- $serverConfig := .Values.serverConfig }}
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-whitelist-json
  namespace: {{ .Release.Namespace }}
data:
  content: {{ $serverConfig.whitelist | toJson }}