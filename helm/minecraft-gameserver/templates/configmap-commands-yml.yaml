---
{{- $serverConfig := .Values.serverConfig }}
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-commands-yml
  namespace: {{ .Release.Namespace }}
data:
  content: {{ $serverConfig.commands | toYaml | nindent 4 }}
