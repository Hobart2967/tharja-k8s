---
{{- $serverConfig := .Values.serverConfig }}
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-bukkit-yml
  namespace: {{ .Release.Namespace }}
data:
  content: {{ $serverConfig.bukkit | toYaml | nindent 4 }}
